% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{tagr_shot_analysis}
\alias{tagr_shot_analysis}
\title{Shot analysis goal probability vs distance pressure shot count}
\usage{
tagr_shot_analysis(
  df,
  feature = c("distance", "pressure", "shot_count"),
  players = NULL,
  add_team = TRUE,
  long_short_only = FALSE
)
}
\arguments{
\item{df}{A TeamTV shots data.frame.}

\item{feature}{One of distance pressure shot_count.}

\item{players}{Optional NULL for team only or a vector length 1 to 3 of player names or shirt numbers.}

\item{add_team}{Logical if true and players is not null include team series too.}

\item{long_short_only}{Logical if true keep only type in c(long short) before fitting.}
}
\value{
A ggplot object.
}
\description{
fits a binomial logistic regression of scoring goal vs miss against one predictor
then plots the observed shots plus the fitted probability curve or fitted points
}
\details{
predictor choice is controlled by feature
\itemize{
\item distance uses distance capped at 10 meters with distance_cap = pmin(distance, 10)
\item pressure uses an ordered factor none then medium then high
\item shot_count is binned as 1 2 3 4+
}

player comparison
\itemize{
\item players can be null for team only or a vector of 1 to 3 players by name or number
\item if players is provided and add_team is true a team series is included for comparison
\item legend shows team and player labels the title never includes player names
}

missing and unknown handling
\itemize{
\item unknown result onbekend is dropped then result must contain both goal and miss
\item rows missing the chosen feature are dropped
\item for pressure onbekend and values outside none medium high are dropped
\item if a player has no shots in a given pressure or shot count level that level is omitted
this avoids predict errors from new unused levels
}

input validation
\itemize{
\item calls validate_teamtv_shots(df) before any processing
}
}
