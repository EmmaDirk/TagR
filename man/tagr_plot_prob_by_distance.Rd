% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{tagr_plot_prob_by_distance}
\alias{tagr_plot_prob_by_distance}
\title{Plot scoring probability vs distance (binomial model)}
\usage{
tagr_plot_prob_by_distance(
  df,
  player = NULL,
  add_team_line = TRUE,
  long_short_only = FALSE
)
}
\arguments{
\item{df}{A TeamTV shots data.frame.}

\item{player}{Optional. Player name (fuzzy match) or shirt number (exact match). Default NULL for all players.}

\item{add_team_line}{Logical. If TRUE and player is not NULL, add a team fitted line for comparison.}

\item{long_short_only}{Logical. If TRUE, keep only type in c("LONG","SHORT") before fitting.}
}
\value{
A ggplot object.
}
\description{
Fits a logistic regression of GOAL vs distance and plots points + fitted curve.
Distance is capped at 10 meters (values above 10 are set to 10).
If player is provided, the plot includes both the player line and the team line,
and colors points so it is clear which points are player vs team.
Optionally restricts to LONG and SHORT shots only.
}
\details{
Input validation is performed using validate_teamtv_shots().
}
